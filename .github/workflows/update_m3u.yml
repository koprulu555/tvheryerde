name: 🚀 M3U Güncelle

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */12 * * *'

jobs:
  update-m3u:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 📂 Repoyu klonla
        uses: actions/checkout@v4

      - name: 🐍 Python kur
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 requests kur
        run: pip install requests

      - name: 🔐 Workers'tan kodu al
        run: |
          curl -s -H "User-Agent: GitHub-Actions-koprulu555" \
               -H "Referer: https://github.com/koprulu555/tvheryerde" \
               "https://tvheryerde-list.koprulu2.workers.dev/kodlar.txt" > worker_code.py

      - name: ▶️ Kodu çalıştır
        run: python worker_code.py

      - name: 📝 Değişiklikleri kontrol et ve gönder
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "🔄 M3U güncellendi" || echo "Değişiklik yok"
          git push || echo "Push gerekmiyor"
